package billboard_viewer;


import java.util.TreeMap;

public abstract class Billboard {



    /*
    public TreeMap<String, String> getBillboardContents(CurrentBillboard billboard) {
        TreeMap<String, String> billboardContents = new TreeMap<>();
        if (billboard.getBillboardMessage() != null) {
            billboardContents.put("message", billboard.getBillboardMessage());
        }
        if (billboard.getBillboardMessage() != null) {
            billboardContents.put("base64Image", billboard.getBillboardBase64Image());
        }
        return billboardContents;
    }
    */



}
